import { Select } from './index';

export default {
  title: 'Components/Select',
};

export const Overview = () => /*html*/ `
  <div>
    <div>
      ${Select({})}
    </div>
    <br />
    <div>
      ${Select({})}
    </div>
  </div>
`;
